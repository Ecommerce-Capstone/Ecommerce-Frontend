export interface IOrderItem {
    id: number,
    orderId: number,
    productId: string,
    productName: string,
    productDescription: string,
    productImages: string,
    price: number
    quantity: number
}